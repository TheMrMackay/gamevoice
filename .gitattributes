# Normalise to LF in the repository.
* text=auto eol=lf

# Windows script hosts are happier with CRLF on disk, and cmd.exe in
# particular can mis-parse a multi-line .cmd saved with bare LF.
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf

*.png binary
*.ico binary
*.onnx binary
